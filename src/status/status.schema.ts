import mongoose, { Document } from 'mongoose';
import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
// import { ProductStatusSchema } from 'src/productCard/productCard.schema';

// @Schema({ collection: 'productstatuses' })
// export class ProductStatus extends Document {
//   @Prop({ required: true })
//   name: string;
// }

// export const ProductStatusModel = mongoose.model<ProductStatus>('ProductStatus', ProductStatusSchema);
